Legacy Python Implementation
============================

This directory archives the pre-C Python implementation. It is no longer part
of the installable `merkmal` Python package.

For now, `tools/generate_c_data.py` imports this code to regenerate the C
built-in data tables from the historical JSON/TSV model files. Treat this as a
migration aid, not as a supported runtime API.
